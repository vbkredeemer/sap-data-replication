# Core dependencies
# Note: pyrfc is no longer used — sap_rfc.py (included in this directory)
#       is a pure-Python ctypes wrapper for sapnwrfc.dll/.so. No pip install needed.
pyodbc>=5.0
paramiko>=3.0

# GUI dependencies
PySide6>=6.5

# Build (optional, for creating standalone .exe)
# pyinstaller>=6.0